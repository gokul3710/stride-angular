import { ProductOfferPipe } from './product-offer.pipe';

describe('ProductOfferPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductOfferPipe();
    expect(pipe).toBeTruthy();
  });
});
