import { ScrollResetDirective } from './scroll-reset.directive';

describe('ScrollResetDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollResetDirective();
    expect(directive).toBeTruthy();
  });
});
